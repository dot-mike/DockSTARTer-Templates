name: Validate

on:
  push:
    paths:
      - 'docs/**'
      - '.github/workflows/validate.yml'
  pull_request:
    paths:
      - 'docs/**'
      - '.github/workflows/validate.yml'

jobs:
  validate-docs:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - name: Validate Documentation
        run: |
          for APPFOLDER in .apps/*; do
              APPNAME=$(basename "${APPFOLDER}")
              APPDEPRECATED=$(grep --color=never -Po "\scom\.dockstarter\.appinfo\.deprecated: \K.*" ".apps/${APPNAME}/${APPNAME}.labels.yml" | sed -E 's/^([^"].*[^"])$/"\1"/' | xargs || echo false)
              if [[ ${APPDEPRECATED} == true ]]; then
                  continue
              fi
              if [[ ! -f "docs/apps/${APPNAME}.md" ]]; then
                  echo "Missing documentation for ${APPNAME}"
                  exit 1
              fi
          done

  # validate-images:
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Checkout
  #       uses: actions/checkout@v4.2.2
  #     - name: Validate Images
  #       run: |
  #         for APPFOLDER in .apps/*; do
  #             APPNAME=$(basename "${APPFOLDER}")
  #             APPDEPRECATED=$(grep --color=never -Po "\scom\.dockstarter\.appinfo\.deprecated: \K.*" ".apps/${APPNAME}/${APPNAME}.labels.yml" | sed -E 's/^([^"].*[^"])$/"\1"/' | xargs || echo false)
  #             if [[ ${APPDEPRECATED} == true ]]; then
  #                 continue
  #             fi
  #             APPIMAGE=$(grep --color=never -Po "\simage: \K.*" ".apps/${APPNAME}/${APPNAME}.x86_64.yml" | sed -E 's/([^:]+):[^:]+/\1/' | xargs || echo false)
  #             APPTAG=$(grep --color=never -Po "\scom\.dockstarter\.appvars\.${APPNAME}_tag: \K.*" ".apps/${APPNAME}/${APPNAME}.labels.yml" | sed -E 's/^([^"].*[^"])$/"\1"/' | xargs || echo false)
  #             echo "Image: ${APPIMAGE}:${APPTAG}"
  #             docker run --rm mplatform/mquery "${APPIMAGE}:${APPTAG}"
  #         done
